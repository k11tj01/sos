<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ViewHealthInsurance" >

    <TextView
        android:id="@+id/providerView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />

    <TextView
        android:id="@+id/policyNoView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/policyOwnerView"
        android:layout_alignParentTop="true"
        android:layout_marginTop="54dp"
        android:text="@string/hello_world" />

    <TextView
        android:id="@+id/policyOwnerView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/providerView"
        android:layout_below="@+id/policyNoView"
        android:layout_marginTop="40dp"
        android:text="@string/hello_world" />

</RelativeLayout>
